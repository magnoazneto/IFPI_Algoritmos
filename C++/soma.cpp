#include <iostream>
//#include <iomanip>
using namespace std;

int main(){
    int a, b, c, d;
    cin >> a >> b >> c >> d;
    int diferenca = (a*b) - (c*d);
    //cout << fixed;
    cout << "DIFERENCA = " << diferenca << endl;
    return 0;
}